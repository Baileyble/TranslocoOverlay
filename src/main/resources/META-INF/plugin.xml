<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>com.baileyble.TranslocoOverlay</id>
    <name>Transloco Overlay</name>
    <vendor url="https://github.com/Baileyble">Baileyble</vendor>

    <description><![CDATA[
        <p>Provides hover-to-view and inline editing for Transloco translation keys in Angular templates.</p>

        <h3>Features</h3>
        <ul>
            <li><b>Hover</b> over translation keys to see their values across all languages</li>
            <li><b>Ctrl+Click</b> on keys in HTML templates to navigate to JSON translation files</li>
            <li><b>Ctrl+Shift+Click</b> on keys in HTML to open multi-language translation editor</li>
            <li><b>Ctrl+Shift+Click</b> on keys in JSON files to find usages in templates</li>
            <li>Edit all language translations in one convenient dialog</li>
            <li>Google Translate integration for auto-translation</li>
            <li>Create new translation keys that don't exist yet</li>
            <li>Autocomplete for translation keys</li>
            <li>Support for Nx monorepo scoped translations</li>
        </ul>

        <h3>Supported Patterns</h3>
        <ul>
            <li><code>{{ 'key' | transloco }}</code> - Pipe syntax</li>
            <li><code>[transloco]="'key'"</code> - Attribute binding</li>
            <li><code>transloco="key"</code> - Direct attribute</li>
            <li><code>t('key')</code> with <code>*transloco</code> directive - Structural directive</li>
        </ul>

        <h3>Open Source</h3>
        <p>This plugin was created with AI assistance (Claude). The source code is open source and available on
        <a href="https://github.com/Baileyble/TranslocoOverlay">GitHub</a>. You are encouraged to review the code,
        report issues, or create your own fork to customize it for your internationalization needs.</p>
    ]]></description>

    <!-- Platform and plugin dependencies -->
    <depends>com.intellij.modules.platform</depends>
    <depends>JavaScript</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Goto declaration handler for Ctrl+Click navigation -->
        <gotoDeclarationHandler
                implementation="com.baileyble.translocooverlay.TranslocoGotoDeclarationHandler"
                order="first"/>

        <!-- Reference contributor for references -->
        <psi.referenceContributor
                implementation="com.baileyble.translocooverlay.TranslocoReferenceContributor"/>

        <!-- Documentation provider for hover tooltips -->
        <documentationProvider implementation="com.baileyble.translocooverlay.TranslocoDocumentationProvider"/>

        <!-- Completion contributor for autocomplete -->
        <completion.contributor
                language="any"
                implementationClass="com.baileyble.translocooverlay.TranslocoCompletionContributor"/>

        <!-- Editor factory listener for Ctrl+Shift+Click editing -->
        <editorFactoryListener
                implementation="com.baileyble.translocooverlay.TranslocoEditorFactoryListener"/>
    </extensions>
</idea-plugin>
